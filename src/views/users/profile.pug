extends ../base
include ../mixins/video

block content
  div.profile__data
    if loggedInUser.avatarUrl === undefined
      img.avatar(src="/uploads/default.png")
    else
      if isHeroku
        img(src=user.avatarUrl).avatar
      else if user.socialOnly
        img(src=user.avatarUrl).avatar
      else
        img(src=`/${user.avatarUrl}`).avatar
    h3=user.name
  div.video-grid
    each x in user.videos
      +video(x)
    else
      span.empty__message 업로드한 동영상이 없습니다!