extends ../base
include ../mixins/video

block content
  div.profile__data
    if loggedInUser.avatarUrl === undefined
      span 😀
    else
      if isHeroku
        img(src=user.avatarUrl).avatar
      else
        img(src= "/" + user.avatarUrl).avatar
    h3=user.name
  div.video-grid
    each x in user.videos
      +video(x)
    else
      span.empty__message No videos found